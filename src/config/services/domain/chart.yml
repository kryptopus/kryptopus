services:
    # Command handler to collect candles
    kryptopus_chart_command_handler_collect_candles:
        class: domain/chart/commandHandler/CollectCandles
        arguments:
            - "@kryptopus_chart_collector_builder"

    # Register collector
    kryptopus_chart_collector_registry:
        class: domain/chart/service/CollectorRegistry

    # Build collector instances
    kryptopus_chart_collector_builder:
        class: domain/chart/service/CollectorBuilder
        arguments:
            - "@kryptopus_chart_collector_registry"
            - "@container"


