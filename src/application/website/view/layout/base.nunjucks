<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8"/>
        <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
        <meta name="viewport" content="width=device-width, initial-scale=1"/>

        <title>{{ documentTitle | default("CombOrder") }}</title>
        <link rel="stylesheet" type="text/css" href="{{ getAssetUrl("website.css") }}"/>
        <script src="{{ getAssetUrl("website.js") }}" defer></script>
    </head>
    <body>
        {# Header #}

        {% block body %}
        {% endblock %}
    </body>
</html>
